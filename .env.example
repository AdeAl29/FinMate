# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/student_finance_tracker?schema=public"

# Optional: direct database URL for Prisma Migrate when DATABASE_URL uses pooling (PgBouncer)
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/student_finance_tracker?schema=public"

# Use a long, random secret in production
JWT_SECRET="replace-with-a-secure-long-random-secret"
